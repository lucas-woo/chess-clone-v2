syntax="proto3";

option go_package = "github.com/lucas-woo/chess-clone-v2/api/authentication/v1/authv1";

package authentication.v1;

message SignUpUserRequest {
  string username = 1;
  string hashed_password = 2;
  string email = 3;
}

message SignUpUserResponse {
  enum SignUpError {
    NIL = 0;
    INVAlID_EMAIL = 1;
    EMAIL_ALREADY_TAKEN = 2;
    INVALID_USERNAME = 3;
    USERNAME_ALREADY_TAKEN = 4;
    INVALID_PASSWORD = 5;
  }
  SignUpError signup_error = 1;
  string session_id = 2;
}

message LoginUserRequest {
  string username = 1;
  string hashed_password = 2;
  string email = 3;  
}

message LoginUserResponse {
  enum LoginError {
    NIL = 0;
    INVAlID_CREDENTIALS = 1;
  }
  LoginError login_error = 1;
  string session_id = 2;
}